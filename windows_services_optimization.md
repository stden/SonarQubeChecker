# Оптимизация служб Windows для разработчика

## 🔴 Службы, которые можно ОТКЛЮЧИТЬ:

### Неиспользуемые сетевые службы:
- **Fax** - факс (если не используете)
- **RemoteRegistry** - удаленный реестр
- **Telephony** - телефония
- **WebClient** - веб-клиент

### Мультимедиа (если не нужны):
- **Themes** - темы (отключит Aero)
- **TabletInputService** - планшет
- **TouchKeyboard** - сенсорная клавиатура

### Старые технологии:
- **Fax**
- **WinRM** - Windows Remote Management
- **RemoteAccess** - удаленный доступ

## 🟡 Службы, которые можно перевести в РУЧНОЙ режим:

### Разработка:
- **Docker Desktop Service** - запускать по требованию
- **PostgreSQL/MySQL** - если используете локально
- **IIS** - если не разрабатываете веб

### Система:
- **Windows Search** - индексирование (замедляет систему)
- **Superfetch** - предзагрузка приложений
- **Bluetooth** - если не используете

## 🟢 Службы, которые НУЖНО оставить:

### Безопасность:
- **Windows Defender**
- **Windows Firewall**
- **SecurityHealthService**

### Система:
- **Audio**
- **DHCP Client**
- **DNS Client**
- **EventLog**
- **Plug and Play**
- **RPC**
- **Task Scheduler**

## 🛠️ Команды для управления службами:

```powershell
# Остановить и отключить службу
Stop-Service -Name "ServiceName" -Force
Set-Service -Name "ServiceName" -StartupType Disabled

# Перевести в ручной режим
Set-Service -Name "ServiceName" -StartupType Manual

# Включить службу
Set-Service -Name "ServiceName" -StartupType Automatic
Start-Service -Name "ServiceName"
```

## ⚠️ ВНИМАНИЕ:
- Отключайте службы постепенно
- Тестируйте систему после изменений
- Создайте точку восстановления перед изменениями